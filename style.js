const num2Handler = () => {
  number2 = secondNum.value;
};

const addsHandler = () => {
  result.innerHTML = parseInt(number1) + parseInt(number2);
};

const subHandler = () => {
  result.innerHTML = parseInt(number1) - parseInt(number2);
};

const mulHandler = () => {
  result.innerHTML = parseInt(number1) * parseInt(number2);
};
